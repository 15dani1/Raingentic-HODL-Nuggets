/**
 * Thin route — renders the frontend Dashboard UI.
 * Owned by: frontend/UI team.
 */
import { Dashboard } from "@/frontend/components/Dashboard";

export default function DashboardPage() {
  return <Dashboard />;
}
