/**
 * Thin route — renders the developer/engineering dashboard UI.
 * Owned by: frontend/UI team.
 */
import { DevDashboard } from "@/frontend/components/DevDashboard";

export default function DevDashboardPage() {
  return <DevDashboard />;
}
